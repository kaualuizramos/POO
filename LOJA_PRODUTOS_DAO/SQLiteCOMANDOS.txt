Bash:
    sudo apt update

    sudo apt install sqlite3 -y

    sqlite3 --version

    sqlite3 teste.db
No arquivo:
    CREATE TABLE usuarios (id INTEGER PRIMARY KEY, nome TEXT);
    INSERT INTO usuarios (nome) VALUES ('João');
    SELECT * FROM usuarios; 
    // Caso queira criar teste.db

Bash:
    .exit